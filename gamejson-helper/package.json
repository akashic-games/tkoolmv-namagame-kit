{
  "name": "gamejson-helper",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "prepare": "npm run build",
    "start": "parcel ./src/index.html --dist-dir ./.run-cache",
    "build": "rimraf ./dist && parcel build ./src/index.html --no-source-maps --public-url ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "UNLICENSED",
  "devDependencies": {
    "@akashic/game-configuration": "^1.10.0",
    "parcel": "^2.10.3",
    "rimraf": "^5.0.1"
  }
}
